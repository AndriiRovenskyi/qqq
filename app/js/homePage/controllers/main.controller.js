app.controller('mainCtrl',['UserService',function (UserService,$scope, $timeout, $rootElement) {
    
var vm=this;

// vm.showCategory=function (link) {
//
// }

    // $rootElement.data("$$ngAnimateState").running = false;
}])